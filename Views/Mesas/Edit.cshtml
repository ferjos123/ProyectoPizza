@model PizzeriaWeb3._1.Models.Mesas

@{
    ViewData["Title"] = "Editar";
}

<h1>Editar</h1>

<h4>Mesas</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdMesas" />
            <div class="form-group">
                <label asp-for="NombreMesas" class="control-label"></label>
                <input asp-for="NombreMesas" class="form-control" />
                <span asp-validation-for="NombreMesas" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Guardar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">¡Volver!</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
